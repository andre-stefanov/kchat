pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "KChat"
include(":shared")
include(":android")
include(":backend")
